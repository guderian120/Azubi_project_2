# Backend API URL - accessible from browser (host network)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Backend API URL - for server-side requests (container network)
BACKEND_API_HOST=http://clms-backend:5000
